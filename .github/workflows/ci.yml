name: CI Pipeline for TDD_zadania

on:
  push:
    branches:
      - TDD_zadania
  pull_request:
    branches:
      - TDD_zadania

jobs:
  lint-and-test:
    name: Lint and Run Tests
    runs-on: ubuntu-latest

    steps:
      # Sprawdzenie kodu z repozytorium
      - name: Checkout repository
        uses: actions/checkout@v3

      # Ustawienie Pythona
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12  # Ustawienie wersji Pythona na 3.12

      # Instalacja zależności bez requirements.txt
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov

      # Uruchomienie testów z raportem pokrycia
      - name: Run tests with coverage
        run: |
          pytest --cov=. tests/

      # Upload raportu pokrycia jako artefakt
      - name: Upload coverage report
        uses: actions/upload-artifact@v3
        with:
          name: coverage-report
          path: coverage.xml

  lint:
    name: Lint Python Code
    runs-on: ubuntu-latest

    steps:
      # Checkout kodu
      - name: Checkout repository
        uses: actions/checkout@v3

      # Ustawienie Pythona dla lintera
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      # Instalacja Pylint
      - name: Install Pylint
        run: |
          python -m pip install --upgrade pip
          pip install pylint

      # Uruchomienie Super-Linter
      - name: Run Super-Linter
        uses: github/super-linter@v4
        env:
          VALIDATE_ALL_CODEBASE: true
          VALIDATE_PYTHON_PYLINT: true
          DEFAULT_BRANCH: TDD_zadania
